name: CI

on: [push]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - name: Copy code to PROD
      run: jsonlint-php -q styles/*.json >/dev/null 2>&1 && ls -l > /tmp/BINGO
